---
- name: Creates directory
  become: true
  file:
    path: ~/ubuntu/web
    state: directory
- name: Extract the artifact
  become: true
  unarchive:
    src: '~/project/artifact.tar.gz'
    dest: '~/ubuntu/web'
- name: Start the web app
  become: true
  shell: |
    cd ~/ubuntu/web
    npm install
    pm2 stop default
    pm2 start npm -- start
